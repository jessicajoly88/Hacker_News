<ul>
  <% @posts.each do |post| %>
    <li><%= link_to post.title, post.url %> <%= link_to "Edit", edit_post_path(post) %> <%= link_to "Delete", post_path(post), data: {confirm: "Really?", method: "delete"} %></li>
  <% end %>

  <p><%= link_to "Add Post", new_post_path %></p>
</ul>
